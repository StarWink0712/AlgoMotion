# Hot 100 补齐进度

[返回 README](../README.md) · [贡献指南](../CONTRIBUTING.md) · [验证记录](VALIDATION.md)

当前已实现 **100 / 100 道**，本项目计划清单已补齐，100 个条目对应 100 个不同题号。2026-09-22 的统一检查中，2509 项单元/API 测试通过；Java（JDK 8）与 Python 各 100 份参考实现、各 591 个用例通过；265 项桌面场景首轮 263 通过、2 个旧格式断言失败，修正断言后相关两组共 26 项复跑全部通过。没有把首轮记录改写成 265/265，也没有再次重跑整套桌面测试。Go 工具链缺失，Go 原生执行以及其他平台仍待验证，详见[验证记录](VALIDATION.md)。

下文各批次保留实施时的历史状态，“待验收”不代表上述检查未发生；最新状态以本段和验证记录为准。开放生成和用户题库不计入预设题数量。不同版本 Hot 100 清单可能变化；本清单和测试结果不代表官方背书或任意输入均正确。

## 已完成

原有 10 道：1、283、11、3、53、206、102、70、322、300。完整题名与方法见 README。

第一批新增：

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 35 | 搜索插入位置 | 左闭右开区间、中点比较、末尾插入边界 |
| 34 | 在排序数组中查找元素的第一个和最后一个位置 | 两轮二分、重复元素的左右边界 |
| 33 | 搜索旋转排序数组 | 有序半段判断、搜索区间收缩 |
| 121 | 买卖股票的最佳时机 | 历史低点、候选收益、最佳买卖日期 |
| 55 | 跳跃游戏 | 可达范围扩张、不可达断点、越过终点的裁剪展示 |

本批均包含 Java/Go/Python 参考代码、严格输入校验、Trace v2 快照及桌面播放/回退检查。股票配对是执行器记录的最佳日期；跳跃范围不是路径，不能读成某条已经选择的跳法。二分的高亮框表示待查元素，半开区间右边界仍可能是插入答案。

第一批运行器位于 `src/engine/presets/search-greedy.ts`，与原有题目共用 recorder，避免把所有新题继续塞进一个大 switch。类别、侧栏数量和探索进度从题库自动推导。没有预存动画视频或所有输入的轨迹。

## 第二批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 49 | 字母异位词分组 | 单词特征、分桶、重复单词的独立身份 |
| 128 | 最长连续序列 | 去重集合、跳过非起点、逐个连接后继 |
| 238 | 除自身以外数组的乘积 | 左右累积、排除自身、乘积合并 |
| 560 | 和为 K 的子数组 | 空前缀、频次查询、多个非空区间、先查后记 |

实现、三语言参考代码、样例/边界/独立对拍及桌面测试用例已编写。本批运行器与参考代码位于 `src/engine/presets/array-hash*.ts`。新分组与前缀画布使用现有 MovingGroup/FlowArrow；连续链复用数组场景。展示数据采用 `variables.view` 内部版本 1，不改变 Trace v2 顶层字段，也不改动生成模式协议。

按当前工作约定，补题阶段只执行快速 TypeScript 编译检查，不重复运行 Vitest、Playwright、三语言执行或完整构建。所有测试集中到余下题目补齐后运行；未跑的测试不能写成通过。最终验收仍需覆盖每一批以及共用组件的回归。

## 第三批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 15 | 三数之和 | 稳定元素身份、固定起点、双指针搜索与去重答案累积 |
| 75 | 颜色分类 | 0/1/2 分区、交换动作、换入元素再次检查 |
| 189 | 轮转数组 | k 取余、整体/前段/后段三次反转 |
| 31 | 下一个排列 | 从右寻找转折、最小增量交换、后缀反转与回绕 |
| 56 | 合并区间 | 数轴排序、闭区间相交、结果边界扩展、单点区间 |

运行器与参考代码在 `src/engine/presets/array-transform*.ts`。数组场景复用交换动画和快照区间条；区间场景按原始 ID 保持来源身份，用独立结果条表示合并过程。Trace v2 顶层结构不变，三元组和区间成员关系使用预设专用 `variables.view` 版本 1。

已编写三语言参考、30 个共享样例/边界用例、独立穷举/连通分量对拍和桌面回放用例。本批只执行 TypeScript 编译检查，未执行这些用例、原生三语言验证、生产构建或浏览器视觉验收。后续正式验收须一并覆盖第二批及共用渲染器的变动。

## 第四批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 20 | 有效的括号 | 最近左括号、配对连线、错配与剩余 |
| 155 | 最小栈 | 值与最小值成对入栈、重复最小值、弹出后恢复 |
| 739 | 每日温度 | 等待下标、严格更暖、距离结算、尚未确定的答案 |
| 84 | 柱状图中最大的矩形 | 非递减栈、左右边界、候选面积、末尾结算 |
| 42 | 接雨水 | 非递增栈、槽底、两墙与逐层注水 |
| 239 | 滑动窗口最大值 | 队首过期、队尾淘汰、相同值保留较新下标 |
| 438 | 找到字符串中所有字母异位词 | 固定长度、频次需求、重叠答案 |
| 76 | 最小覆盖子串 | 覆盖后收缩、重复字符、最优区间与当前窗口分离 |
| 32 | 最长有效括号 | −1 边界、无法匹配的右括号、连续有效段 |
| 394 | 字符串解码 | 外层前缀保存、次数累积、内层展开与拼接 |

本批扩大为 10 道。实现按 `src/engine/presets/stack-window*.ts` 拆分，接入现有目录、三语言参考、输入编辑器与播放器。`StackWindowScene` 共用 FlowArrow 和 Web Animations，入栈项从操作区移动，出栈项有明确的移出标记；长输入在内部横向滚动，栈按稳定身份换行。柱形画布区分真实柱子、已结算雨水、当前候选矩形及最终最佳矩形，不预存动画。新增“栈”“单调栈”类别与“困难”难度。

已编写 67 个共享样例/边界用例、每题 120 组独立对拍、快照/输入/面积/频次不变量及 13 个专项桌面场景；现有通用题库测试也会自动包含新增题目。本批两次 TypeScript 编译通过；这些行为用例、三语言原生执行、浏览器动画和生产构建均未执行。不能将代码检查或已编写的用例当作验收通过。

## 第五批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 160 | 相交链表 | 双头切换、独有前缀与共享尾部、按身份相遇 |
| 234 | 回文链表 | 找中间、后半段反转、对应比较、失败后也恢复 |
| 141 | 环形链表 | 慢一步快两步、移动后判断相遇、自环 |
| 142 | 环形链表 II | 第一阶段相遇、回到头节点、同步定位入口 |
| 21 | 合并两个有序链表 | 较小头节点接入、同值先选 A、复用原节点 |
| 2 | 两数相加 | 低位优先、缺失位按零、逐位新建、最终进位 |
| 19 | 删除链表的倒数第 N 个结点 | 哨兵、固定间距、绕过正确节点 |
| 24 | 两两交换链表中的节点 | 保存后缀、三步重连、奇数尾部保留 |
| 25 | K 个一组翻转链表 | 满组探测、逐条反转、接回组头、短尾不动 |
| 138 | 随机链表的复制 | 原新两组身份、分配与连线两阶段、random 自指与交叉 |

运行器、目录、讲解和三语言代码位于 `src/engine/presets/linked-list*.ts`。`LinkedListScene` 复用现有 FlowArrow/MovingGroup 与节点样式，增加按来源分区的有限节点布局；不递归遍历 next，因此自环和共享尾部不会导致布局递归。所有节点使用真实的执行器 ID；哨兵、被绕过节点、新建节点、next、random、已断开连接与复制映射有独立标识。映射线不是实际指针。原来的反转链表与树画布不改动。

新增 68 个共享样例/边界用例、每题 120 组独立对拍、身份/恢复/深拷贝不变量及 13 个专项桌面场景。三语言工具已支持共享对象构造、环入口序列化和随机链表的隔离身份检查，回文适配器还检查原连接恢复。这些测试均未执行。TypeScript 初次检查发现可空指针推断问题，修正后通过，包括桌面用例与最终画布改动；不能据此声称算法或原生三语言已验收。

## 第六批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 94 | 二叉树的中序遍历 | 左侧入栈、出栈访问、转向右子树 |
| 104 | 二叉树的最大深度 | 空树返回 0、子树高度向父节点返回、最长根叶路径 |
| 226 | 翻转二叉树 | 同一节点身份移动、左右归属交换、空孩子 |
| 101 | 对称二叉树 | 成对镜像位置、空位与结构不匹配、短路返回 |
| 543 | 二叉树的直径 | 单侧高度与双侧边数分离、不经过根的最长路径 |
| 108 | 将有序数组转换为二叉搜索树 | 中点区间、真实创建、子树返回后接入 |
| 98 | 验证二叉搜索树 | 全部祖先边界、开区间、重复值与深层违规 |
| 230 | 二叉搜索树中第 K 小的元素 | 访问而非入栈时计数、第 k 个提前停止 |
| 199 | 二叉树的右视图 | 每层固定队列长度、右侧可见不等于右孩子链 |
| 114 | 二叉树展开为链表 | 保存原右子树、左移右、清空 left、节点排成右链 |

实现位于 `src/engine/presets/tree*.ts`；新的树画布复用 FlowArrow/MovingGroup 和既有节点视觉。原紧凑层序解析器抽到 `src/engine/tree-data.ts`，`run.ts` 保留原导出与原层序题限制。新树题允许最多 31 个编码项、15 个实际节点、5 层，以容纳稀疏结构；构造题使用严格递增数组。槽位布局不会递归遍历展开过程的暂时共享连接，也不会因右链变长产生指数宽度。

新增 60 个共享样例/边界用例，每题 120 组独立对拍、节点身份/结构/高度/路径不变量及 13 个专项桌面场景。独立检查使用对象树、所有起点的图距离、右优先 DFS、递增中序和树高平衡性质；不只比较预设样例。三语言工具新增树序列化和翻转/展开的身份与连接检查。全部行为、三语言与浏览器用例均未执行。本批三次 TypeScript 检查通过；共享解析器和旧预设仍须参加最终回归。

## 第七批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 105 | 从前序与中序遍历序列构造二叉树 | 前序游标、中序分段、实际分配节点与子树接入 |
| 437 | 路径总和 III | 先查后记、重复前缀频次、命中的向下路径与退出撤销 |
| 236 | 二叉树的最近公共祖先 | BFS 节点身份、目标自身为祖先、左右子树返回值 |
| 124 | 二叉树中的最大路径和 | 单侧向上贡献、双侧候选、全负树与真实最优路径 |
| 148 | 排序链表 | 快慢指针找中点、断链、稳定归并与身份不变 |
| 23 | 合并 K 个升序链表 | 链表组分治、较小头接入、重复值的稳定顺序 |
| 146 | LRU 缓存 | key → 节点哈希表、双向指针、命中移首、更新及尾部淘汰 |
| 46 | 全排列 | 候选占用、选择路径、撤销与独立答案副本 |
| 78 | 子集 | 空集、下标递增、每个搜索节点都是答案 |
| 39 | 组合总和 | 重复使用候选、非递减路径、剩余值与有序剪枝 |

树与链表复用原画布，只增加前序输入、前缀频次、目标标记和分治调用条。新 `SearchCacheScene` 使用已有 FlowArrow/MovingGroup：回溯将候选连向当前选择路径，撤销单独标记，已收集答案独立保存并内部滚动；LRU 节点按身份移动，展示真实 next/prev，淘汰节点与当前缓存分开。没有执行或生成模型代码，不增加普通预设依赖。

构造题要求两序列互异、一致且至多 5 层；最近公共祖先的 p/q 为非空节点的紧凑 BFS ID，允许同一节点，不是值或含 null 的数组位置。最大路径和拒绝空树。排序链表至多 12 节点；K 链至多 4 条、合计 16 节点。LRU 容量 1–6、操作至多 24 次。全排列至多 5 项、子集至多 7 项；组合总和至多 5 个互异正候选、target 为 0–10，以控制完整搜索的轨迹与画布规模。

已编写 60 个共享结果/边界用例、每题 120 组独立对照（共 1200 组）、严格输入与节点/指针/路径/快照不变量，以及 13 个专项桌面场景。三语言适配器检查排序/合并后的原节点身份、重复值稳定性，并按原对象定位公共祖先。仅 TypeScript 编译已执行；这些用例、原生三语言、桌面动画和完整构建仍待统一验收，详见验证记录。

## 第八批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 17 | 电话号码的字母组合 | 数字到字母候选、逐位选择、撤销与答案副本 |
| 22 | 括号生成 | 合法前缀、左右计数恢复、不能选择的原因 |
| 131 | 分割回文串 | 连续片段、双端回文检查、切分与撤销 |
| 79 | 单词搜索 | 四邻接、格子不可复用、占用恢复、独立保存成功路径 |
| 51 | N 皇后 | 逐行摆放、同列与对角线冲突来源、撤回占用 |
| 200 | 岛屿数量 | BFS 连通块、入队即标记、对角不连通 |
| 994 | 腐烂的橘子 | 多源起点、固定每轮队列长度、感染分钟与不可达 |
| 207 | 课程表 | 前置边方向、入度下降、零入度队列、剩余阻塞不等于全在环中 |
| 54 | 螺旋矩阵 | 已读路径、四条剩余边界、单行单列不重复 |
| 73 | 矩阵置零 | 原始零写入行列标记、首行首列独立标志、避免错误传播 |

实现、参考与讲解按 `src/engine/presets/exploration*.ts` 拆分。`ExplorationScene` 使用既有 FlowArrow/MovingGroup 与浅色风格，支持字符串选择条、格子路径、皇后冲突线、橘子图形、BFS/拓扑队列以及课程依赖图。选择、撤销、感染、解除依赖、边界收缩和标记转移均由当前执行快照驱动，不预先展示未来答案。旧预设和生成模式协议不改写。

输入上限用于完整且可读的演示：号码至多 3 位；括号 n≤4；回文串至多 7 个 ASCII 字母；单词至多 4 个 ASCII 字母且棋盘至多 4×4；N 皇后 n≤5。其他网格至多 6×6，课程至多 10 门/24 条不重复前置边。空号码返回 []，零对括号返回 [""]，空字符串回文分割返回 [[]]，空单词定义为存在，空矩阵允许 []、不允许非空矩阵内出现空行。平台更大输入不在本演示支持范围内。

已编写 60 个共享样例/边界用例、每题 120 组独立对照（合计 1200 组）、输入/快照/路径/传播/标记不变量及 13 个专项桌面场景。独立参考使用笛卡尔积、括号位掩码、切分掩码、路径广搜、列排列、并查集、同步整网格传播、传递闭包、方向步进和原始零集合；不以同一个运行器的结果充当独立依据。三语言工具新增字符棋盘转换、单词搜索输入恢复及矩阵置零原地身份检查。全部行为/三语言/桌面用例仍待执行，仅类型检查通过，详见验证记录。

## 第九批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 41 | 缺失的第一个正数 | 值 x 放入 x−1、换入值继续检查、重复值停止条件 |
| 48 | 旋转图像 | 转置、逐行反转、同值元素的独立移动身份 |
| 74 | 搜索二维矩阵 | 虚拟展开下标映射、二分排除、严格行间条件 |
| 240 | 搜索二维矩阵 II | 行列非递减、右上角比较、候选矩形收缩 |
| 153 | 寻找旋转排序数组中的最小值 | 中点比右端、最小值区间、保留 mid 的分支 |
| 4 | 寻找两个正序数组的中位数 | 短数组切口、左半边总数、空边界与交叉关系 |
| 208 | 实现 Trie（前缀树） | 逐字复用、缺边分配、前缀与完整单词、空串 |
| 215 | 数组中的第 K 个最大元素 | 小顶堆、重复值排名、超限弹出最小项 |
| 347 | 前 K 个高频元素 | 逐项频次、最弱候选堆顶、同频优先级 |
| 295 | 数据流的中位数 | 左大顶堆/右小顶堆、上浮下沉、节点跨堆移动 |

实现按 `ordering*.ts` 与 `structures*.ts` 拆分。原数组画布复用于归位和区间二分，新的 `OrderingScene` 使用同一套 MovingGroup/FlowArrow 表达矩阵元素交换与两数组切口；空边界不伪装成真实元素。`StructuresScene` 根据实际堆数组绘制父子关系，展示暂存节点、频次和真实 Trie 字符边。调整中的堆序和数量可能暂时不满足最终不变量，明确在 `heap-ready` / 插入完成后恢复，不把堆画成预排序数组。

一般数组最多 24 项，矩阵最多 6×6。矩阵 I 要求按行展开严格递增；矩阵 II 只要求每行每列非递减。旋转图像必须为正方形；寻找最小值要求非空互异旋转升序数组；两个中位数数组合计至少 1 项、最多 24 项。Trie 最多 12 次操作，每词最多 5 个小写字母，插入字符总数≤24；空前缀始终存在，空单词需显式插入。数据流最多 24 次操作/16 次插入，空流查询被拒绝。高频题固定同频时数值小者优先，结果按频次降序、同频数值升序排列。

已编写 60 个共享样例/边界用例、每题 120 组独立对照（合计 1200 组）、身份/候选/切口/堆序/双堆平衡/前缀结构不变量，以及 13 个桌面场景。独立依据使用集合、坐标旋转、线性查找、完整排序、独立计数和单词集合。三语言参考包含手写堆，原生适配器检查归位不丢失元素及旋转保留矩阵/行身份。仅 TypeScript 编译已运行并通过；这些行为、三语言执行、动画和布局检查全部待统一验收。

## 第十批：已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 45 | 跳跃游戏 II | 扫描整层、下一跳最远边界、不可达检测 |
| 763 | 划分字母区间 | 字母最后位置、区间扩展、最早合法切分 |
| 118 | 杨辉三角 | 边界 1、双父状态汇入、未知位置留白 |
| 198 | 打家劫舍 | 前缀状态、取与不取、非相邻选择 |
| 279 | 完全平方数 | 逐金额比较平方数候选、重复使用与还原分解 |
| 139 | 单词拆分 | 可达前缀与字典后缀同时成立、逐步保存拆分 |
| 152 | 乘积最大子数组 | 最大/最小双状态、负数反转、非空最优区间 |
| 416 | 分割等和子集 | 倒序更新、旧轮次来源、按输入位置区分两组 |
| 62 | 不同路径 | 上方/左方路径数相加、不伪造单条选定路径 |
| 64 | 最小路径和 | 前驱候选代价、包含起终点、一条最优路径 |

实现集中在 `dp-greedy*.ts`，新增可信 `StateDPScene`，复用 MovingGroup/FlowArrow。输入元素与 DP 寄存器分开布局，依赖保存实际来源值及轮次；合法但未采用的候选不标为非法。长表换行/内部滚动，回退与跳转停止前向转移动画并恢复快照。乘积见证使用乘号，金额/平方数用加号，格子路径用箭头。

输入上限用于可读演示：跳跃数组与划分字符串≤24；杨辉三角≤10 行；房屋≤16 个、金额≤100；平方数 n≤40；拆分串≤14、字典≤8 词；乘积数组≤12、值在 -5 到 5；等和数组≤12 个正数、总和≤60；两类路径网格≤6×6。跳跃不可达返回 -1，单元素需 0 跳；空拆分串可达；1×1 不同路径为 1；最小路径和包括起终点且仅向右/下。详细协议以各题输入提示为准。

已编写 60 个共享样例/边界用例、每题 120 组独立对照（合计 1200 组）、状态/依赖/轮次/路径/输入与快照不变量，以及 13 个桌面场景。独立依据使用 BFS、所有切分掩码、组合数、非相邻子集枚举、字典拼接、连续区间枚举、子集掩码与全部单调路径。三语言适配器接入字典字符串数组及网格矩阵。仅 TypeScript 类型检查已运行；行为、原生三语言、桌面布局和动画测试都留待统一验收。

## 第十一批：最后六题已实现，待验收

| 题号 | 题目 | 演示重点 |
| --- | --- | --- |
| 5 | 最长回文子串 | 奇数/偶数中心、相同扩展、不同停止、最左最长结果 |
| 1143 | 最长公共子序列 | 字符坐标、对角匹配、上/左取优、回溯后缀 |
| 72 | 编辑距离 | 空前缀边界、替换/删除/插入、保留零代价、最优对齐 |
| 136 | 只出现一次的数字 | 8 位补码、异或前后、真实配对抵消 |
| 169 | 多数元素 | 候选重选、加票/抵消、票数与频次的区别 |
| 287 | 寻找重复数 | i→nums[i]、快指针中间跳、相遇点与入口分离 |

新增执行器、参考代码和讲解集中在 `final-six*.ts`。LCS 和编辑距离复用 `StateDPScene`，以可选字段增加字符坐标、状态回溯路径及对齐列；已有 DP 帧不需要这些字段。`EssentialsScene` 复用数组与链表画布，补充异或位图、未抵消元素和数组函数图语义。输入变化仍走已有执行与回放机制，不涉及模型配置或容器执行。

回文串最多 16 个小写 ASCII 字符，LCS/编辑距离两串各≤8，均允许空串。回文多解取最左；LCS 仅返回长度、附带一条可行最优子序列。编辑距离每次插入/删除/替换代价为 1，不能交换相邻字符算一次。异或数组≤23、值在 -128..127，恰好一个单次值、其余均成对。多数数组≤24，必须有严格超过一半的值；剩余票数不当成总频次。重复数数组长度 2–13、值在 1..长度−1，恰好一种值重复，可重复超过两次，不修改输入。复杂度注明核心算法与验证、快照、可视化注释的区别；编辑参考保留对齐前插的额外开销。

已编写 36 个共享样例/边界用例、每题 120 组独立对照（合计 720 组）、输入/快照/状态/抵消/指针跳转不变量及 9 个桌面场景。独立依据为所有子串、子序列掩码、全部编辑对齐、频次统计及排序；编辑对齐还检查源串消费、目标生成与操作计数。原生适配器额外检查寻找重复数不修改输入。仅类型检查与静态题号清点已执行，行为、原生三语言、浏览器和全量回归尚未执行。

## 后续验证

补题阶段及本机首轮统一检查已经完成，已修复跳跃区间越界、负零轨迹归一化、字符串/格式化 JSON/分类计数断言和 Java 8 兼容问题。后续仍需在具备 Go 1.21+ 的环境执行 Go 参考代码、验证其他平台并按需复测真实模型/沙箱。当前没有后台自动执行任务。普通预设不需要模型密钥或 Docker，Go 原生验证也不是日常使用的前提。

## 每道题的最终验收标准

- 原创简述、明确输入输出和边界语义，不复制完整平台题面。
- 确定性执行器及语义插桩，输出来源与当前输入一致。
- 三语言参考实现与语义行映射；原生验证必须如实报告，不能用 TypeScript 执行冒充 Java/Go/Python 验证。
- 可解释的因果动画、自定义输入、时间轴和回退；不只增加一个返回结果函数或变色表格。
- 独立参考对拍、非法输入及样例/边界测试；不以能播放代替算法正确性检查。
- 桌面浏览器实测、完整回归及文档更新；普通预设不引入模型或 Docker 依赖。
